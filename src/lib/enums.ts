export const enum GameStatus {
  Overview,
  Playing,
}

export const enum ModalContent {
  Rules,
  Stats,
}

export const enum WordleStatus {
  Answered,
  Completed,
  InvalidWord,
  InvalidTurn,
  Ongoing,
  Restart,
}
