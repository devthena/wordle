export const enum GameStatus {
  ModePick,
  Create,
  Room,
  SoloStart,
  SoloEnd,
  CoopStart,
  CoopEnd,
}

export const enum WordleStatus {
  Answered,
  Completed,
  InvalidWord,
  InvalidTurn,
  Ongoing,
  Restart,
}
